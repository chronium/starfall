---
id: SF-0006
title: Adopt coordinator-family source-consumption policy
track: SF
milestone: M0
dependsOn:
- BUILD-0002
createdAt: 2026-08-02T09:46:18.2953030Z
modifiedAt: 2026-08-02T09:51:40.6050350Z
---

Adopt the approved family-checkout build policy without integrating shared presentation yet.

## Acceptance criteria

- Define one overridable `ChronoFallFamilyRoot` property in Starfall's root MSBuild configuration. Its default resolves the canonical coordinator parent directory; no product project receives a shared reference in this task.
- Replace the repository-wide prohibition on coordinator source with an executable narrow rule: only `Starfall.Client` may later reference the approved coordinator presentation projects through `$(ChronoFallFamilyRoot)`.
- Continue to reject literal repository escapes, absolute paths, arbitrary property-rooted external references, Royale references, and parent imports.
- Preserve the existing Starfall product graph and keep Content, Protocol, Simulation, World, Editor, and BalanceLab free of coordinator presentation, SDL, GPU, rendering, and client dependencies.
- Update Starfall `AGENTS.md`, relevant repository-local skills, architecture/workflow/roadmap wiki pages, and `CLIENT-0006` so the canonical full-client environment is the coordinator family checkout while PM, source history, product architecture, build/release ownership, and headless isolation remain Starfall-owned.
- Keep completed `BUILD-0002` unchanged as historical evidence. Do not mutate coordinator tasks, add shared project references, cook or copy content, introduce NuGet distribution, implement rendering, or advance the coordinator gitlink.
- Validate Starfall PM, Debug and Release builds/tests, architecture gates, diffs, and repository ownership; commit only Starfall-owned files and stop.

## Notes

- 2026-08-02 09:51 UTC - Implemented the approved Starfall-only family-source policy correction. Added the single overridable `ChronoFallFamilyRoot` property, which evaluates to `/Users/chronium/Developer/ChronoFall/` in the canonical checkout, and expanded the architecture suite to permit only the three approved coordinator character-presentation project paths for future `Starfall.Client` use. The executable policy rejects headless consumers, Royale, direct SDL3-CS, arbitrary property roots, absolute paths, and literal repository escapes. No shared ProjectReference, coordinator import, generated content, package/feed dependency, renderer, gameplay behavior, coordinator PM mutation, or gitlink update was added.

  Updated Starfall `AGENTS.md`, architecture/build/review skills, architecture overview, repository workflow, bootstrap roadmap, and todo task `CLIENT-0006`. Repository/product/PM/release ownership remains independent while the coordinator family checkout becomes the canonical full-client environment. Completed `BUILD-0002` remains unchanged as historical evidence.

  Validation: PM doctor passed before and after changes; linked-family inspection returned all three projects available/readable/trusted with zero warnings; `ChronoFallFamilyRoot` evaluated to the coordinator root; restore passed; Debug and Release builds each completed with zero warnings and errors; all 14 architecture tests passed in both configurations; `dotnet format --verify-no-changes --no-restore` and `git diff --check` passed. Project-reference inspection confirmed the Starfall product graph is unchanged and contains no coordinator, Royale, or SDL references. Royale remained clean, no coordinator-owned file or task changed, and only the expected unstaged Starfall gitlink difference remains in the parent. No visual output or owner visual gate applied.